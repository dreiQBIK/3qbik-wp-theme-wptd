{% extends "base.twig" %}

{% block content %}
   <div class="l_home" data-template="page-modules.twig">
      <div class="container sp-md">
         <header>
            <h1>{{ __( 'Styleguide', 'wptd' ) }}</h1>
         </header>
         <div class="l_builder">
         {% for module in styleguide_modules %}
            {% include module %}
         {% endfor %}
         </div>
      </div>
   </div>
{% endblock %}
