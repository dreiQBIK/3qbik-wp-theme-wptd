{% extends "base.twig" %}

{% set content = post.meta('tinymce') %}

{% block content %}
   <div class="l_page" data-template="tpl-builder.twig">
      <header class="container">
         <h1 class="heading-1">{{ post.title }}</h1>
      </header>
      <div class="container">
         <div class="row">
            <div class="col col-12">
               <div class="l_builder">
                  {% include '/partials/layouts/l_builder.twig' %}
               </div>
            </div>
         </div>
      </div>
   </div>
{% endblock %}
