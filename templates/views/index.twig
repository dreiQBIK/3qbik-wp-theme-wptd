{% extends "base.twig" %}

{% block html_head_container %}
   {% include 'html-header-noindex.twig' %}
</head>
{% endblock %}

{% block content %}
   <div class="l_page pt-400 pt-md-600 pb-600 pb-md-800" data-template="index.twig">

      <header class="container">
         <h1>{{ post.title }}</h1>
      </header>

      <section class="container">
         <div>{{ post.content }}</div>
      </section>

   </div>
{% endblock %}
